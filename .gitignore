.env

# Python / legacy
.venv/
**/__pycache__/
legacy/python-mealplan/.venv/

# Backend
backend/target/
backend/uploads/
backend/application-local.properties
backend/.idea/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/dev-dist/
frontend/*.local
frontend/.DS_Store

# Editor
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs/
*.log

# Per-repo output of the setup-matt-pocock-skills skill (issue tracker, triage
# labels, domain doc layout). Kept locally so the engineering skills still work
# on this machine, not pushed — the "wayfinder"/"triage" workflow language in
# these files reads as agent-swarm internals rather than project documentation.
docs/agents/
