# Copy to ".env" in the repo root (Compose loads it automatically).
# Optional overrides for local development — both fall back to "petar" /
# "localdev" if unset, which is fine for a throwaway local db and nothing else.

# POSTGRES_USER=petar
# POSTGRES_PASSWORD=localdev
