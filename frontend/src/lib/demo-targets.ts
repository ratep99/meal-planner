import type { Macros } from "@/types/recipe";

/** Placeholder daily targets until profile data is wired into recipe views. */
export const DEMO_DAILY_TARGETS: Macros = {
  kcal: 2300,
  protein: 180,
  carbs: 220,
  fat: 70,
};
